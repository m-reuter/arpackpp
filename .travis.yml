sudo: false

language: cpp

compiler: gcc

os:
  - linux
  - osx

addons:
  apt:
    packages:
      - gfortran
      - libopenblas-dev
      - liblapack-dev
      - libarpack2-dev

env:
  - SUPERLU=ON BLAS=SYSTEM

before_install:
  # OSX
  - if [ "$TRAVIS_OS_NAME" == "osx" ]; then brew update ; fi
  - if [ "$TRAVIS_OS_NAME" == "osx" ]; then brew install homebrew/science/arpack homebrew/science/superlu ; fi

before_script:
  # linux needs SuperLU5.0 from source (APT libsuperlu-dev is still 4.3)
  - if [ "$TRAVIS_OS_NAME" == "linux" ] && [ "$SUPERLU" == "ON" ]; then ./install-superlu.sh ; fi
  - mkdir build
  - cd build 
  - cmake -D SUPERLU=$SUPERLU ..

script:
  - make
